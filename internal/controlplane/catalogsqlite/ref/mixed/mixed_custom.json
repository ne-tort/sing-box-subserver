{
  "schema_version": 1,
  "tag": "mixed_custom",
  "protocol": "mixed",
  "aliases": [
    "mixed-custom",
    "mixed-constructor"
  ],
  "short_name": "Mixed custom",
  "status": "lab",
  "custom_preset": true,
  "i18n": {
    "ru": {
      "title": "Mixed конструктор",
      "description": "HTTP+SOCKS inbound; outbound как SOCKS/HTTP. Plain или TLS."
    },
    "en": {
      "title": "Mixed constructor",
      "description": "HTTP+SOCKS inbound; client outbound SOCKS/HTTP. Plain or TLS."
    }
  },
  "traits": [
    "tcp",
    "custom"
  ],
  "demux_hints": {
    "network": [
      "tcp"
    ],
    "looks_like": "http",
    "alpn": [],
    "sni_required": false,
    "first_bytes": "HTTP methods, SOCKS5, or TLS",
    "compatible_with_demux": true
  },
  "scores": {
    "dpi": 1,
    "speed": 8,
    "mobile": 7,
    "setup": 3
  },
  "cred_fields": [
    "username",
    "password"
  ],
  "param_fields": [
    "tls_mode"
  ],
  "optional_param_fields": [
    "fingerprint",
    "outbound_type"
  ],
  "param_meta": {
    "tls_mode": {
      "type": "enum",
      "widget": "select",
      "enum": [
        "none",
        "tls"
      ],
      "default": "none",
      "ui_group": "tls",
      "ui_order": 1,
      "title": {
        "en": "TLS mode",
        "ru": "Режим TLS"
      },
      "description": {
        "en": "Plain vs TLS-wrapped utility proxy.",
        "ru": "Plain или обёртка TLS."
      }
    },
    "fingerprint": {
      "type": "enum",
      "widget": "select",
      "enum": [
        "chrome",
        "firefox",
        "safari",
        "edge",
        "ios",
        "android",
        "random"
      ],
      "default": "chrome",
      "ui_group": "tls",
      "ui_order": 2,
      "visible_when": [
        {
          "key": "tls_mode",
          "equals": "tls"
        }
      ],
      "title": {
        "en": "uTLS fingerprint",
        "ru": "uTLS fingerprint"
      },
      "description": {
        "en": "Outbound uTLS fingerprint when tls_mode=tls.",
        "ru": "uTLS fingerprint на outbound при tls_mode=tls."
      }
    },
    "outbound_type": {
      "type": "enum",
      "widget": "select",
      "enum": [
        "socks",
        "http"
      ],
      "default": "socks",
      "ui_group": "core",
      "ui_order": 10,
      "title": {
        "en": "Client outbound type",
        "ru": "Тип outbound клиента"
      },
      "description": {
        "en": "Subscription outbound type against mixed inbound.",
        "ru": "Тип outbound в подписке к mixed inbound."
      }
    }
  },
  "requirements": {
    "tls_profile": true
  },
  "inbound_template": {
    "type": "mixed",
    "tag": "{{tag}}",
    "listen": "{{listen}}",
    "listen_port": 0,
    "users": [],
    "tls": {
      "enabled": true,
      "server_name": "{{server}}",
      "alpn": [
        "h2",
        "http/1.1"
      ]
    }
  },
  "outbound_template": {
    "type": "socks",
    "tag": "{{tag}}",
    "server": "{{server}}",
    "server_port": 0,
    "username": "{{user.username}}",
    "password": "{{user.password}}",
    "tls": {
      "enabled": true,
      "server_name": "{{server}}",
      "alpn": [
        "h2",
        "http/1.1"
      ],
      "utls": {
        "enabled": true,
        "fingerprint": "{{param.fingerprint}}"
      }
    }
  },
  "client_notes": {
    "outbound": "outbound_type socks|http rewritten at materialize; client talks to mixed inbound"
  }
}
