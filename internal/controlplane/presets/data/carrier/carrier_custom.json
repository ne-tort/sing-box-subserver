{
  "schema_version": 1,
  "tag": "carrier_custom",
  "protocol": "carrier",
  "aliases": [
    "carrier-custom"
  ],
  "short_name": "Carrier custom",
  "status": "lab",
  "i18n": {
    "ru": {
      "title": "Carrier конструктор",
      "description": "SFU room URL + опциональный token; provider через params."
    },
    "en": {
      "title": "Carrier constructor",
      "description": "Lab Carrier: pick provider/room/token knobs. Prefer a concrete SFU preset when possible."
    }
  },
  "traits": [
    "tcp",
    "shared_auth",
    "wg_star",
    "sfu",
    "no_registration",
    "jitsi"
  ],
  "demux_hints": {
    "network": [
      "tcp"
    ],
    "looks_like": "raw_tcp",
    "alpn": [],
    "sni_required": false,
    "first_bytes": "carrier underlay",
    "compatible_with_demux": false
  },
  "scores": {
    "dpi": 7,
    "speed": 5,
    "mobile": 4,
    "setup": 3
  },
  "cred_fields": [
    "device_id"
  ],
  "inbound_template": {
    "type": "carrier",
    "tag": "{{tag}}",
    "provider": "jitsi",
    "auth": "shared",
    "link": {
      "room": "{{param.room}}",
      "transport": "datachannel",
      "password": "{{peer.password}}",
      "key": "{{param.key}}"
    },
    "lifecycle": {
      "enabled": true,
      "keepalive": true,
      "liveness_interval": "15s",
      "liveness_timeout": "5s",
      "liveness_failures": 3,
      "reconnect_backoff": "2s",
      "reconnect_backoff_max": "30s"
    }
  },
  "outbound_template": {
    "type": "carrier",
    "tag": "{{tag}}",
    "provider": "jitsi",
    "link": {
      "room": "{{param.room}}",
      "transport": "datachannel",
      "password": "{{peer.password}}",
      "device_id": "{{user.device_id}}",
      "key": "{{param.key}}"
    },
    "lifecycle": {
      "enabled": true,
      "keepalive": true,
      "liveness_interval": "15s",
      "liveness_timeout": "5s",
      "liveness_failures": 3,
      "reconnect_backoff": "2s",
      "reconnect_backoff_max": "30s"
    }
  },
  "requirements": {
    "build_tag": "with_carrier"
  },
  "client_notes": {
    "room": "обязателен в bindings[].params.room; self-host = свой Jitsi host в URL",
    "transport": "datachannel|seichannel (vp8channel запрещён для jitsi)"
  },
  "peer_secret_fields": {
    "password": "password"
  },
  "param_fields": [
    "room"
  ],
  "param_meta": {
    "room": {
      "type": "string",
      "widget": "text",
      "title": {
        "en": "Room URL",
        "ru": "URL комнаты"
      },
      "description": {
        "en": "Optional constructor override for room.",
        "ru": "Опциональный параметр конструктора: room."
      }
    },
    "token": {
      "type": "string",
      "widget": "text",
      "title": {
        "en": "Token",
        "ru": "Токен"
      },
      "description": {
        "en": "Optional constructor override for token.",
        "ru": "Опциональный параметр конструктора: token."
      }
    },
    "provider": {
      "type": "enum",
      "widget": "select",
      "enum": [
        "jitsi",
        "telemost",
        "wbstream",
        "jitsi_sei"
      ],
      "default": "jitsi",
      "title": {
        "en": "Provider",
        "ru": "Провайдер"
      },
      "description": {
        "en": "Carrier SFU provider.",
        "ru": "SFU-провайдер Carrier."
      }
    },
    "key": {
      "type": "string",
      "widget": "text",
      "ui_group": "carrier",
      "ui_order": 60,
      "title": {
        "en": "Room key",
        "ru": "Ключ комнаты"
      },
      "description": {
        "en": "Optional Carrier room key/token.",
        "ru": "Опциональный ключ/токен комнаты Carrier."
      }
    }
  },
  "custom_preset": true,
  "optional_param_fields": [
    "key",
    "provider",
    "token"
  ]
}
